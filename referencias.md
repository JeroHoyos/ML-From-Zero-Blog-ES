# Referencias

Given a set of data points ${x^{(1)}, \ldots, x^{(m)\}}$ associated to a set of outcomes\
${y^{(1)}, \ldots, y^{(m)\}}$, we want to build a classifier that learns how to predict\
$y$ from $x$.
